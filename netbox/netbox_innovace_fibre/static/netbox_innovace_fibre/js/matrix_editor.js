(function () {
    const root = document.getElementById('innovace-matrix-editor');
    if (!root) {
        return;
    }

    root.textContent = 'Matrix editor scaffold loaded. Interactive mapping UI will be added next.';
})();
